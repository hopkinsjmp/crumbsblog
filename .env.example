# No environment variables are required to run this site.
# The CMS is managed via Sveltia CMS at /admin/ using GitHub OAuth.
# OAuth credentials are stored as Cloudflare Worker environment variables, not here.
